<h1>Listing patrimonies</h1>

<table class="table table-bordered">
  <thead>
    <tr>
      <th>Name</th>
      <th>Identifier</th>
      <th>Status</th>
      <th>Description</th>
      <th>Responsible</th>
      <th>Room</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @patrimonies.each do |patrimony| %>
      <tr>
        <td><%= patrimony.name %></td>
        <td><%= patrimony.identifier %></td>
        <td><%= patrimony.status %></td>
        <td><%= patrimony.description %></td>
        <td><%= patrimony.responsible.name %></td>
        <td><%= patrimony.room.locale %></td>
        <td><%= link_to 'Mostrar', patrimony %></td>
        <td><%= link_to 'Editar', edit_patrimony_path(patrimony) %></td>
        <td><%= link_to 'Deletar', patrimony, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Novo Patrimônio', new_patrimony_path %>
